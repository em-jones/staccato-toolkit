/**
 * Mock authentication constants.
 * Used when OPENPORT_AUTH_MOCK=true.
 */

export const MOCK_USER_EMAIL = "dev@openport.local";
export const MOCK_USER_NAME = "Dev User (mock)";
/** Intentionally non-secret — only used by the mock adapter in local dev. */
export const MOCK_PASSWORD = "mock-password-do-not-use-in-production";
