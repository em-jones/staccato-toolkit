# Open QA Core UI

Types and default implementations for the Open QA Core UI, including permissions, stores, and shared
utilities. This package is designed to be used across both the web and terminal interfaces of the
Open QA platform.
